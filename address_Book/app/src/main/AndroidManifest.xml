<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.address_book">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Address_Book">
        <activity android:name=".RegisterPeopleActivity"></activity>
        <activity
            android:name=".AddressListActivity"
            android:label="@string/title_activity_address_list"
            android:theme="@style/Theme.Address_Book.NoActionBar" />
        <activity android:name=".FindPWActivity" />
        <activity android:name=".FindIDActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>